# Admin Interface

The library `starlette-admin` is used for the admin interface. It allows you to manage your database models in a more user-friendly way. Accessible at `/admin`.

The individual admin pages are registered in `main.py`.

For more information refer to the [Starlette-Admin documentation](https://jowilf.github.io/starlette-admin/).